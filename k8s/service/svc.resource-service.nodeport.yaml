apiVersion: v1
kind: Service
metadata:
  name: resource-service
  namespace: k8s-program
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: resource-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      nodePort: 30080
      targetPort: 8080