apiVersion: v1
kind: Service
metadata:
  name: song-service
  namespace: k8s-program
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: song-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      nodePort: 30081
      targetPort: 8081